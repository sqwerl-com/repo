import * as React from 'react'

/**
 * Toolbar component for a component that allows users to navigate through a hierarchy.
 */
const NavigationToolbar = (): React.JSX.Element => {
  return <nav />
}

export default NavigationToolbar
